import { ListenDirective } from './listen.directive';

describe('ListenDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenDirective();
    expect(directive).toBeTruthy();
  });
});
