import { BindDirective } from './bind.directive';

describe('BindDirective', () => {
  it('should create an instance', () => {
    const directive = new BindDirective();
    expect(directive).toBeTruthy();
  });
});
